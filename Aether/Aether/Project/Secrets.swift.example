import Foundation

/// Template for `Secrets.swift`. Copy this file to `Secrets.swift` and fill in
/// real values. The real `Secrets.swift` is gitignored — never commit it.
enum Secrets {
    /// OpenAI Chat Completions key. Used by CodeGenerator (gpt-4o).
    static let openAIKey = "REPLACE_ME_OPENAI_KEY"

    /// Gemini 2.0 Flash key. Used by GeminiClient for "hey jarvis" Q&A.
    static let geminiKey = "REPLACE_ME_GEMINI_KEY"

    /// ElevenLabs TTS key. Used by JarvisVoice for the JARVIS voice.
    static let elevenLabsKey = "REPLACE_ME_ELEVENLABS_KEY"
    static let elevenLabsVoiceId = "REPLACE_ME_ELEVENLABS_VOICE_ID"
}
